export enum JWT_AUDIENCE {
	API_ACCESS = 'API_ACCESS',
	API_REFRESH = 'API_REFRESH',
	PASSWORD_RESET = 'PASSWORD_RESET',
	INVITATION = 'INVITATION'
}

export enum PASSPORT_NAME {
	LOCAL = 'local',
	JWT_API = 'jwt-api',
	JWT_PASSWORD_RESET = 'jwt-password-reset',
	JWT_INVITATION = 'jwt-invitation'
}

export enum MESSAGE_TYPE {
	ERROR = 'ERROR',
	WARNING = 'WARNING',
	SUCCESS = 'SUCCESS',
	INFO = 'INFO'
}

